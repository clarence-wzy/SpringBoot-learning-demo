spring:
  cloud:
    config:
      discovery:
        service-id: configserver
        enabled: true
      name: configclient
      label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/